Every async operation must handle:

- loading
- success
- empty
- error

Never leave a blank screen.

Always show a friendly error message.

Never expose raw Firebase errors to users.

Log technical details only during development.