Never trust client-side validation.

Always enforce security using Firebase Security Rules.

Never expose secrets.

Never hardcode API keys outside environment variables.

Always validate Firestore permissions.

Authentication does not equal authorization.

Only authorized admins may access protected resources.

Never grant admin privileges automatically.

Always deny access by default.