Before creating ANY new component:

Search the project first.

If a reusable component already exists:

Reuse it.

Do not duplicate.

If the component can be reused in another feature:

Move it into:

src/components

Otherwise:

Keep it inside:

src/features/<feature>/components